info:
  name: ipGateway/getIpOutputStatus
  type: http
  seq: 16

http:
  method: POST
  url: https://{{HOSTNAME}}/board/1/ui/firewall-gui/board/1/api/jsonrpc
  body:
    type: json
    data: |
      {
        "jsonrpc": "2.0",
        "method": "ipGateway:1.51/status/GetIpOutputStatus",
        "params": {
          "ids": []
        },
        "id": "GetIpOutputStatus"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
