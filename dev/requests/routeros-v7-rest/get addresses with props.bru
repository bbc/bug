meta {
  name: get addresses with props
  type: http
  seq: 4
}

get {
  url: http://{{HOSTNAME}}/rest/ip/address?.proplist=address
  body: none
  auth: inherit
}

params:query {
  .proplist: address
}

settings {
  encodeUrl: true
  timeout: 0
}
