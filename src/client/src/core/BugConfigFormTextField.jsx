import React from "react";
import TextField from "@mui/material/TextField";

const BugConfigFormTextField = (props) => {
    return <TextField variant="standard" {...props} />;
};

export default BugConfigFormTextField;
