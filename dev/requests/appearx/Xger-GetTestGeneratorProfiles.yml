info:
  name: Xger/GetTestGeneratorProfiles
  type: http
  seq: 10

http:
  method: POST
  url: https://{{HOSTNAME}}/mmi/service_encoderpool/api/jsonrpc
  body:
    type: json
    data: |
      {
        "jsonrpc": "2.0",
        "method": "Xger:2.31/testGeneratorProfile/GetTestGeneratorProfiles",
        "params": {},
        "id": "GetTestGeneratorProfiles"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
