meta {
  name: get addresses with filter
  type: http
  seq: 5
}

get {
  url: http://{{HOSTNAME}}/rest/ip/address?.id=*1
  body: none
  auth: inherit
}

params:query {
  .id: *1
}

settings {
  encodeUrl: true
  timeout: 0
}
