'use strict';

// import dockerode

module.exports = async (panelId) => {
    try {
        // dockerode start
    } catch (error) {
        logger.warn(`panel-start: ${error.trace || error || error.message}`);
    }

}