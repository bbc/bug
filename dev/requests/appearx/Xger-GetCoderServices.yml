info:
  name: Xger/GetCoderServices
  type: http
  seq: 11

http:
  method: POST
  url: https://{{HOSTNAME}}/mmi/service_encoderpool/api/jsonrpc
  body:
    type: json
    data: |
      {
        "jsonrpc": "2.0",
        "method": "Xger:2.31/coderService/GetCoderServices",
        "params": {},
        "id": "GetCoderServices"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
