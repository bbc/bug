import React from "react";
import TextField from "@mui/material/TextField";

const ConfigFormTextField = (props) => {
    return <TextField variant="standard" {...props} />;
};

export default ConfigFormTextField;
