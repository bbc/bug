info:
  name: board/GetInputServices
  type: http
  seq: 2

http:
  method: POST
  url: https://{{HOSTNAME}}/mmi/api/jsonrpc
  body:
    type: json
    data: |
      {
        "jsonrpc": "2.0",
        "method": "board:4.2/services/GetInputServices",
        "params": {
          "query":{"value":{"servicePresence":"ONLY_PRESENT"}}
        },
        "id": "GetInputServices"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
