meta {
  name: Device list
  type: http
  seq: 3
}

get {
  url: https://{{HOSTNAME}}/api/device/list
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

settings {
  encodeUrl: true
  timeout: 0
}
