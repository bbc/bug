info:
  name: Xger/GetColorProfiles
  type: http
  seq: 8

http:
  method: POST
  url: https://{{HOSTNAME}}/mmi/service_encoderpool/api/jsonrpc
  body:
    type: json
    data: |
      {
        "jsonrpc": "2.0",
        "method": "Xger:2.31/colorProfile/GetColorProfiles",
        "params": {},
        "id": "GetColorProfiles"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
