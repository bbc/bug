import { render } from "@testing-library/react";
import BugPasswordTextField from "./BugPasswordTextField";

describe("BugPasswordTextField", () => {
    it("renders", () => {
        render(<BugPasswordTextField />);
    });
});
